import { Button, Container, Row } from "react-bootstrap";

const Masjid = () => {
    

    return (
        <Container>
            <Button variant="primary">Add Masjid</Button>
        </Container>
    );
};

export default Masjid;